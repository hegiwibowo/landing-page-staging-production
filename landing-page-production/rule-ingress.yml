apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: landing-page-ingress
  namespace: lp-production
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: pd-lp.hegiwibowo.xyz
    http:
      paths:
      #- path: /belajar
      - backend:
          serviceName: landing-page
          servicePort: 80